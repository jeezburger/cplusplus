#include <iostream>
using namespace std;
int main() {
  float a, b, c;
  cout << "enter base and height: ";
  cin >> a >> b;
  c = (a * b) / 2;
  cout << "area of the right angled triangle is " << c;
  return 0;
}
